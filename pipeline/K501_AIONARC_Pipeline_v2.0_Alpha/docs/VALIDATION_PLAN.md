# Validation Plan — V2.0 Alpha

Required checks: direct external QH256 v2.0 compile, t9 exact 32-byte vector, record encode/decode, H_record verification, byte-native path preservation, nullable-state distinction, deterministic second-run equality, mutation locality, chain linkage/break detection, and ASan/UBSan execution.

PASS is local to the alpha implementation and does not promote any alpha rule to canonical status.
