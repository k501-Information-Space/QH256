# Repository Resolution Notes

## External metadata drift detected

The currently indexed DEV article for the QH256 C implementation contains an older ORCID identifier in its embedded text. The current project identifier supplied for this repository state is:

`0009-0004-3275-9545`

The same historical article also references older repository/publication identifiers. These differences should be treated as publication-history metadata, not silently rewritten as if they were part of the original artifact.

## Repository policy

Use the current canonical identifiers in new repository files and future releases.

Preserve historical external content as historical content.

When practical, add a short correction/update note to future public releases rather than altering old release artifacts.
